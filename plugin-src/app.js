import React from 'react';
import ReactDOM from 'react-dom';
import styled from '@emotion/styled';

const BigText = styled.h1`
  color: red;
`;

ReactDOM.render(
  <BigText>Hello World asdfasds</BigText>,
  document.getElementById("root") 
);
